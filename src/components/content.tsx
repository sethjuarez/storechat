import { Box } from "@primer/react";
import { Prompts, Documents } from "@components";

const Content = () => {
  return (
    <Box className="source">
      <Prompts />
      <Documents />
    </Box>
  );
};

export default Content;
